# FLOVER-FLIX 메이킹기록

프로미스나인(fromis_9) 팬 영상 아카이브 **FLOVER-FLIX** 를 처음 시작한 순간부터
지금까지의 제작 과정을 정리한 기록이다. 배포 주소: `https://parkhaas.github.io/fp-pilot/`
저장소: `github.com/parkhaas/fp-pilot`

---

## 0. 시작 — 무엇을, 왜

레퍼런스 사이트 [SCENE-FLIX](https://adam-yam.github.io/SCENE-FLIX/) 가 마음에 들어
"이 방식대로 fromis_9 팬페이지를 만들고 싶다"는 요청으로 시작했다.

- **비영리 팬 아카이브**: 음악방송 · 직캠 · 자체콘텐츠 · 스페셜무대 · Shorts 를 한 곳에서
  모아보는 정적 사이트. 영상 자체는 저장하지 않고 YouTube 임베드만 사용.
- **제약 조건**: 빌드 도구 없음(순수 HTML/CSS/JS), 무료 인프라(GitHub Pages), 데이터는
  주기적으로 자동 갱신.
- **데이터**: `data/videos.json` · `data/meta.json` · `data/members.json` 세 개의 정적
  JSON 파일을 프론트엔드가 `fetch()` 로 읽는 구조. 서버·DB 없음.

---

## 1. 데이터 수집 파이프라인

### 1-1. API 우선, 그다음 무료 백업

처음엔 "공식 API 우선"이라는 원칙대로 YouTube Data API v3 로만 수집하는
`crawlers/youtube_crawler.py` 를 만들었다. 채널 handle → channelId → uploads
재생목록 해석, 재생목록·검색(search.list) 수집, 상세조회 필터링, 제목 기반 멤버
추정까지 한 스크립트에 담았다.

그런데 오래된 영상까지 전체 백필을 하려니 `search.list` 가 호출당 **100 유닛**이라
일일 쿼터(10,000)가 금방 바닥났다. 그래서 **키·쿼터가 필요 없는 yt-dlp 스크래핑
크롤러** `crawlers/collect_ytdlp.py` 를 추가로 만들어, 대량 백필은 yt-dlp 로,
정기적인 소량 증분은 API 로 나눠 맡기는 이원 구조를 잡았다.

### 1-2. Shorts 는 아예 다른 문제였다

YouTube 에는 "이 영상은 Shorts 다"를 알려주는 메타데이터 필드가 없다(API·yt-dlp
공통). 처음엔 영상 길이로 판정하려 했지만, 세로로 찍은 3분짜리 직캠까지 Shorts로
오분류되는 문제가 있었다. 여러 차례 테스트 끝에 신뢰 가능한 신호를 정리했다:

1. **신호 #1(가장 확실)** — 영상 URL 이 `youtube.com/shorts/<id>` 형태 = YouTube 가
   직접 분류한 것. 채널의 `/shorts` 탭을 통째로 긁으면 이 신호로 걸러진다.
2. **신호 #2** — 임의의 영상 ID를 `youtube.com/shorts/<id>` 로 HEAD 요청했을 때
   200(유지)이면 Shorts, 303(→`/watch`로 리다이렉트)이면 아니다.
3. **신호 #3(보조)** — 3분 이내 **and** 세로 비율(height>width)인 후보만 재분류
   (`--shorts-aspect`, 느려서 opt-in).

참고로 **키워드·해시태그 검색으로는 Shorts 가 결과에 안 나온다**는 것도 실측으로
확인했다 — `#shorts` 로 80개를 검색해도 Shorts 는 0개였다. 그래서 "채널 `/shorts`
탭"이 사실상 유일하게 믿을 수 있는 Shorts 소스다.

### 1-3. 사고 — 정기 수집이 데이터를 지웠다

가장 큰 사고. `update-data.yml` (GitHub Actions, 12시간마다)이 API 크롤러를
`--since 최근30일` 로 돌리도록 되어 있었는데, **크롤러가 매 실행마다 `videos.json`
을 통째로 새로 만드는 구조**였다. "이번에 다시 찾은 영상"과 "생략 설정된 일부
소스"만 남기고 나머지는 버렸다. 검색 범위가 30일로 좁혀지자 그 밖의 오래된
음악방송·직캠·스페셜무대가 통째로 사라지고, API 크롤러엔 애초에 Shorts 소스가
없어서 Shorts 279개도 전멸했다 — **영상 1,412개가 하루 만에 386개로 줄어드는
사고**로 이어졌다.

복구·재발 방지:
- 문제의 자동 커밋을 `git revert` 로 되돌려 데이터 복원(히스토리 재작성 없이).
- 크롤러 로직 수정 — **증분(`--since`) 실행은 이번에 재수집되지 않은 기존 영상을
  전부 보존**하도록 변경(삭제는 `--since` 없는 전체 실행에서만). API가 애초에
  못 만드는 분류(`shorts`)는 전체 실행에서도 항상 보존.
- `update-data.yml` 에 **"Guard against data loss"** 스텝 추가 — 직전 커밋 대비
  영상 수가 10% 넘게 줄면 커밋하지 않고 워크플로를 실패시킨다. 같은 사고가 다시
  나도 조용히 배포되지 않는다.

### 1-4. 최종 구조 — 영상/쇼츠 이원화

이후 "API 정기 실행에도 Shorts 를 같이 채워달라"는 요청으로 `collect_ytdlp.py`
의 `--only` 옵션을 `video`(재생목록+검색, Shorts 제외) / `shorts`(`/shorts` 탭만)
/ `all` 로 정리했다.

```
정기(GitHub, 12시간마다):  youtube_crawler.py --since 30일전 (API, 영상)
                          → collect_ytdlp.py --only shorts (yt-dlp, best-effort)
                          → 데이터 급감 가드 → 커밋 → Pages 재배포

전체 백필(로컬):           collect_ytdlp.py --only video → --only shorts (순차)
```

GitHub Actions IP는 yt-dlp 봇 차단을 자주 맞기 때문에 Shorts 수집 스텝은
`continue-on-error: true` 로 best-effort 처리 — 차단당해도 기존 Shorts 는 그대로
유지된 채 워크플로는 계속 돈다. 이 구조는 실제로 운영 중이며(`meta.json` 의
`generator` 가 `"youtube-api + yt-dlp(shorts)"` 로 두 소스가 함께 찍힘), 영상 수는
1,412 → 1,418 → 1,426(2026-09-10 기준)으로 삭제 없이 꾸준히 늘고 있다.

---

## 2. 화면 구조 / UX

- **좌측 Drawer 내비게이션**: SCENE-FLIX 를 참고해 상단 탭 대신 왼쪽 드로어로.
  1단 목록에서 하위 분류가 있는 항목을 누르면 그 옆에 **2단 패널이 붙어서** 뜨는
  구조(오버레이가 아니라 나란히), 반투명 플로팅 스타일.
- **카테고리 버튼 → Grid**: 가로 스크롤 없이 카테고리를 눌러 분기하고 결과는 항상
  그리드로 나열.
- **About(소개) 페이지가 시작 화면**: 파라미터 없이 접속하면 곧 소개 페이지. 영상
  통계를 카테고리별/연도별 도넛 차트로 보여준다.
- **멤버 분류**: 데뷔 9인 전원을 분류하되, 화면엔 현재 5인(송하영·박지원·이채영·
  이나경·백지헌)을 기본 노출하고, 탈퇴 4인(이새롬·노지선·이서연·장규리)은
  펼쳐야 보이게 — 탈퇴 역순으로 정렬.
- **"멤버별 보기" 내비게이션**: 처음엔 상단 필터바의 칩(chip) 형태였다가, "기간별
  보기"와 완전히 같은 방식(subBy 서브메뉴)으로 nav 하위 메뉴로 옮겼다. 현재 멤버
  5인과 탈퇴 멤버 4인 사이엔 구분선을 넣었다. `member` 를 쿼리스트링·필터 상태의
  1급 차원으로 편입해서 카테고리와 자유롭게 조합(`?cat=music_show&member=…`)
  가능해졌다.
- **정렬 위치**: 처음엔 별도 필터바에 있던 정렬 셀렉트를, 카테고리 이름이 표시되는
  헤더 줄 우측으로 옮겼다.
- **`songOf()` 곡명 파서**: 방송사 무대·직캠 제목은 "곡-그룹" · "그룹-곡" · 따옴표
  곡명 · 대시 없는 팬캠 표기가 뒤섞여 있어, 곡 서브메뉴에 곡명 대신 멤버명·그룹명이
  섞여 들어가는 버그가 있었다. 치명적인 버그 하나는 최종 검증 정규식(`\W`)이
  **한글을 전부 "비단어 문자"로 취급**해서 순한글 곡명(하얀 그리움, 유리구두 등)을
  통째로 걸러내고 있었던 것 — 이를 고치고 그룹/멤버/촬영수식어 제거, 곡명 표기
  통일(KOR/ENG), 1회성 노이즈 숨김까지 다시 짰다.

---

## 3. 디자인

전역 지침(Toss/Apple 톤, Tailwind CDN, Pretendard, Lucide 아이콘)에 맞춰 라이트/
다크 테마 토글이 있는 전면 리뉴얼을 진행했다.

**여기서 한 번 방향이 꺾였다.** Tailwind Play CDN(`cdn.tailwindcss.com`)을 붙였는데,
로컬 프리뷰 샌드박스의 CSP 가 CDN 런타임(JIT/eval)을 막아서 완전히 언스타일드로
렌더링됐다 — 이 환경에서 검증이 불가능했다. Tailwind Play CDN 은 애초에 "프로덕션
비권장"이기도 해서, **같은 Toss/Apple 톤을 직접 작성한 CSS 토큰 시스템**
(`:root` 라이트 / `html.dark` 다크, 뉴트럴 팔레트 + 핑크 액센트 하나, 큰 라운드,
부드러운 그림자)으로 다시 만들었다. 폰트(Pretendard)·아이콘(Lucide)만 CDN 을
쓰고 레이아웃은 CDN 에 의존하지 않는다. 이 결정은 프로젝트 메모리에 "fp-pilot 은
Tailwind 로 바꾸지 않는다"로 남겨서, 전역 지침보다 프로젝트 결정이 우선하도록
해뒀다.

1단 Drawer 는 리뉴얼 과정에서 상하 16px 여백을 둔 플로팅 패널이 됐다가, "예전엔
화면 높이를 꽉 채웠는데"라는 지적으로 다시 풀 스크린 높이로 되돌렸다.

---

## 4. 배포 파이프라인

- **GitHub Pages**, `deploy.yml`(push to main) + `update-data.yml`(정기 데이터
  갱신 + 배포)의 2-워크플로 구조.
- 봇의 `git push` 는 `GITHUB_TOKEN` 특성상 다른 워크플로를 못 깨우기 때문에,
  `update-data.yml` 안에서 데이터 커밋 이후 Pages 배포까지 직접 수행하도록 접었다.
- **Node.js 20 지원 종료 경고**: 액션 버전을 최신 메이저로 올렸는데도 경고가
  안 없어졌다 — 원인은 `actions/upload-pages-artifact@v4` 가 **composite 액션이라
  내부에서 `actions/upload-artifact@v4.6.2`(Node 20)를 호출**하고 있었던 것.
  최상위 태그만 봐서는 안 보이는 문제였다. `@v5`(내부 핀이 Node 24 로 올라간 버전)
  로 교체해 해소했다.

---

## 5. 그 외 사고와 수습

- **허락 없이 커밋·푸시**: 기능을 완성했다고 임의로 커밋·푸시했다가 사용자에게
  지적받았다. 이후 "이 저장소에서는 명시적으로 요청받을 때만 커밋·푸시한다"를
  프로젝트 메모리에 저장해 재발을 막았다.
- **GitHub Contributors 어트리뷰션**: 모든 커밋이 회사 이메일로 돼 있어 GitHub
  Contributors 에 작업자 본인이 아니라 Claude 공동저자만 표시되는 문제가 있었다.
  42개 커밋 전부의 author/committer 를 개인 이메일로 재작성하고
  `--force-with-lease` 로 재푸시해서 해결했다(Claude 공동저자 표기는 사용자
  선택으로 유지).
- **`@fromis_9` 쇼츠 채널 정리**: `/shorts` 탭이 없는 채널을 `shortsChannels` 에
  넣어둬서 실행마다 에러가 나던 걸 제거.

---

## 6. 현재 상태 (2026-09 기준 스냅샷)

- 영상 총 **1,426개** (`workdol 31 · shorts 284 · music_show 563 · stage_fancam 216
  · plunen_idanjang 82 · sp_ch9 128 · musinsa_seongsugi 22 · pulmuone_ei2 29` 등)
- 데이터 생성기: `youtube-api + yt-dlp(shorts)` — 영상/쇼츠 이원 파이프라인이
  실제로 자동 운영 중.
- 전체 커밋 78개, 그중 상당수가 `chore(data): refresh archive` 자동 갱신 커밋
  (12시간 주기 정상 동작 확인).
- 로컬 개발용 `serve.bat` 추가 — 더블클릭 한 번으로 `python -m http.server` 기동
  + 기본 브라우저 오픈.

---

## 7. 돌아보며

- **무료·무빌드 제약 안에서도** 정적 파일 3개(JSON)만으로 검색·필터·조합
  가능한 아카이브를 만들 수 있었다. 데이터 파이프라인의 핵심 교훈은
  **"증분 수집은 절대 기존 데이터를 암묵적으로 삭제해선 안 된다"** — 이 원칙을
  코드(병합 로직)와 CI(가드 스텝) 양쪽에 박아 넣은 뒤에야 안심할 수 있는
  파이프라인이 됐다.
- Shorts 판정처럼 **플랫폼이 메타데이터로 안 주는 정보**는 결국 "그 플랫폼이
  스스로 어떻게 분류해서 보여주는가"(URL 형태)를 신뢰하는 게 가장 견고했다 —
  길이·비율 같은 휴리스틱은 보조 신호로만 썼다.
- 디자인은 지침(Tailwind)과 실제 환경 제약(샌드박스 CSP)이 충돌했을 때, 지침의
  *의도*(Toss/Apple 톤)는 지키되 수단은 상황에 맞게 바꾸는 쪽을 택했다.
